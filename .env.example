# ==============================================
# Aura / Dieta Positiva - Environment Configuration
# ==============================================

# ----------------------------------------------
# SYSTEM KEYS (Project Infrastructure)
# ----------------------------------------------
# Supabase Configuration (Required for persistence)
# Get these from your Supabase Project Settings -> API
NEXT_PUBLIC_SUPABASE_URL="https://your-project.supabase.co"
NEXT_PUBLIC_SUPABASE_ANON_KEY="your-anon-key"
SUPABASE_SERVICE_ROLE_KEY="your-service-role-key"

# Node Environment (development/production/test)
NODE_ENV="development"
PORT="3000"

# ----------------------------------------------
# USER KEYS (AI Models)
# ----------------------------------------------
# xAI (Grok) API Key (Primary - Recommended)
# Used for 'meta-agent' task interpretation and chat.
# Fast and capable. Get a key at https://console.x.ai
XAI_API_KEY="xai-..."

# Groq API Key (Fallback)
# Used if XAI_API_KEY is not configured.
# Get a free key at https://console.groq.com
GROQ_API_KEY="gsk_..."

# Anthropic API Key (For execution-agent coding tasks)
# Used for complex reasoning and code generation.
ANTHROPIC_API_KEY="sk-ant-..."

# ----------------------------------------------
# AGENT CONFIGURATION
# ----------------------------------------------
# Execution Mode ('mock' or 'real')
# 'mock': Simulates execution with random delays (Safe)
# 'real': Executes actual shell commands/files (Use with Caution)
EXECUTION_MODE="mock"
